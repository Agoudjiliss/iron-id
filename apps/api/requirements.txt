# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Validation
pydantic==2.7.1
pydantic-settings==2.2.1

# Auth
bcrypt==4.1.3
python-jose[cryptography]==3.3.0

# Redis & Celery
redis==5.0.4
celery==5.4.0

# HTTP client
httpx==0.27.0

# Cloud storage (S3-compatible for Cloudflare R2)
# boto3 is pulled in transitively by aioboto3 — do NOT pin it separately
aioboto3==12.4.0

# Cryptography (C2PA signing — ECDSA P-256)
cryptography==42.0.7

# Rate limiting
slowapi==0.1.9

# Clerk webhook verification
svix==1.20.0

# PayPal (REST API via httpx — no heavy SDK needed)
# httpx already listed above — no additional package required

# Monitoring
sentry-sdk[fastapi]==2.3.1

# Utilities
python-multipart==0.0.9
python-dotenv==1.0.1
structlog==24.1.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
