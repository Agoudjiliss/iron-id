# Backend base URL – used at build time to derive the WebSocket URL (wss://…).
# HTTP API calls use relative paths (/api/…) and are routed to the backend by
# the Vite dev-proxy (development) or the Vercel rewrites in vercel.json (production).
# Local dev:  http://localhost:8000
# Production: https://iron-id-ea601dce55ce.herokuapp.com
VITE_API_URL=https://iron-id-ea601dce55ce.herokuapp.com
