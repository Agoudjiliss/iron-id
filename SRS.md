# وثيقة متطلبات البرمجيات (SRS)
# نظام Iron-ID لمصادقة الوسائط الرقمية

---

**الإصدار:** 1.0  
**التاريخ:** مارس 2026  
**المشروع:** Iron-ID – منصة توثيق وحماية الوسائط الرقمية  
**الحالة:** نهائي  

---

## جدول المحتويات

1. [المقدمة](#1-المقدمة)
   - 1.1 [الهدف من النظام](#11-الهدف-من-النظام)
   - 1.2 [نطاق المشروع](#12-نطاق-المشروع)
   - 1.3 [التعريفات والمصطلحات](#13-التعريفات-والمصطلحات)
   - 1.4 [المراجع](#14-المراجع)
2. [الوصف العام للنظام](#2-الوصف-العام-للنظام)
   - 2.1 [منظور النظام](#21-منظور-النظام)
   - 2.2 [وظائف النظام الرئيسية](#22-وظائف-النظام-الرئيسية)
   - 2.3 [فئات المستخدمين](#23-فئات-المستخدمين)
   - 2.4 [القيود والافتراضات](#24-القيود-والافتراضات)
3. [المتطلبات الوظيفية](#3-المتطلبات-الوظيفية)
4. [المتطلبات غير الوظيفية](#4-المتطلبات-غير-الوظيفية)
   - 4.1 [الأداء](#41-الأداء)
   - 4.2 [الأمان](#42-الأمان)
   - 4.3 [القابلية للاستخدام](#43-القابلية-للاستخدام)
   - 4.4 [القابلية للتوسع](#44-القابلية-للتوسع)
5. [مخططات حالات الاستخدام](#5-مخططات-حالات-الاستخدام)
6. [هيكلية النظام المقترحة](#6-هيكلية-النظام-المقترحة)
7. [السيناريوهات الرئيسية لاستخدام النظام](#7-السيناريوهات-الرئيسية-لاستخدام-النظام)

---

## 1. المقدمة

### 1.1 الهدف من النظام

يهدف نظام **Iron-ID** إلى توفير منصة متكاملة لمصادقة الوسائط الرقمية وحمايتها من التزوير والتلاعب بالذكاء الاصطناعي. يُتيح النظام للمنشئين والصحفيين والعلامات التجارية إثبات أصالة محتواهم الرقمي من خلال توليد بصمات رقمية مشفرة وتوقيعات تحققية تثبت هوية الوسيطة وسلامتها منذ لحظة إنشائها.

يُعالج النظام مشكلة تنامي ظاهرة "التزييف العميق" (Deepfake) والمحتوى المُولَّد بالذكاء الاصطناعي الذي بات يُهدد مصداقية الوسائط الرقمية في مجالات الصحافة والفن والتجارة.

**الأهداف الرئيسية:**

- **الحماية:** إضافة طبقات حماية رقمية متعددة على ملفات الوسائط (صور، صوت، فيديو).
- **التحقق:** السماح لأي طرف بالتحقق من أصالة ملف وسائط وكشف أي تعديلات طرأت عليه.
- **التتبع:** ربط الوسائط بهوية صاحبها من خلال بيانات وصفية موثقة.
- **الشفافية:** عرض النتائج التقنية التفصيلية لعملية التحقق بطريقة مفهومة.

---

### 1.2 نطاق المشروع

يغطي هذا النظام الوظائف التالية:

| النطاق | التفاصيل |
|--------|----------|
| **نوع التطبيق** | تطبيق ويب أحادي الصفحة (SPA) مع دعم التطبيقات التدريجية (PWA) |
| **طبقة الواجهة الأمامية** | React 18 + TypeScript مبني بـ Vite 5 |
| **الخادم الخلفي (Backend)** | خدمة REST + WebSocket على منصة Heroku |
| **المنصات المدعومة** | المتصفحات الحديثة (Chrome، Firefox، Safari، Edge)، الأجهزة المحمولة |
| **أنواع الوسائط المدعومة** | الصور (JPEG، PNG، GIF، WebP، TIFF)، الصوت (MP3، WAV، FLAC، OGG، AAC)، الفيديو (MP4، MOV، MKV، WebM، AVI) |
| **الحد الأقصى لحجم الملف** | 500 ميغابايت |
| **اللغات المدعومة** | الفرنسية، الإنجليزية، العربية (مع دعم الكتابة من اليمين إلى اليسار) |
| **النشر** | Vercel CDN (الواجهة الأمامية) + Heroku (الخلفية) |

**خارج النطاق:**

- إدارة حسابات المستخدمين أو تسجيل الدخول (لا يوجد نظام مصادقة حاليًا).
- تخزين الملفات الأصلية على المدى الطويل.
- إنشاء شهادات قانونية رسمية معتمدة من جهات تشريعية.

---

### 1.3 التعريفات والمصطلحات

| المصطلح | التعريف |
|---------|--------|
| **SRS** | وثيقة متطلبات البرمجيات (Software Requirements Specification) |
| **Iron-ID** | اسم المنظومة البرمجية لمصادقة الوسائط الرقمية |
| **Deepfake (التزييف العميق)** | محتوى مرئي أو مسموع مُولَّد أو معدَّل بالذكاء الاصطناعي بهدف تضليل المشاهد |
| **C2PA** | معيار "اتحاد استناد محتوى وأصالة المحتوى" (Coalition for Content Provenance and Authenticity)، معيار صناعي مفتوح لتوثيق مصدر الوسائط |
| **العلامة المائية (Watermark)** | إشارة رقمية غير مرئية مدمجة في الملف لإثبات الملكية وتتبع التعديلات |
| **SHA-256** | خوارزمية تجزئة رقمية آمنة تُولِّد بصمة فريدة لكل ملف |
| **PSNR** | نسبة الإشارة إلى الضوضاء (Peak Signal-to-Noise Ratio) – مقياس جودة يقيس الفرق البصري بين الملف الأصلي والمحمي |
| **Delta-E** | مقياس الفارق اللوني بين صورتين – يُستخدم لتقييم مدى التأثير المرئي للحماية |
| **WebSocket** | بروتوكول اتصال ثنائي الاتجاه في الوقت الفعلي لتتبع تقدم المعالجة |
| **PWA** | التطبيق التدريجي للويب (Progressive Web App) – تطبيق ويب قابل للتنزيل على الجهاز |
| **SPA** | تطبيق الصفحة الواحدة (Single Page Application) |
| **RTL** | الكتابة من اليمين إلى اليسار (Right-to-Left) – مستخدمة للغة العربية |
| **REST API** | واجهة برمجية تعتمد على بروتوكول HTTP وفق معمارية REST |
| **Vercel** | منصة استضافة للواجهات الأمامية تعتمد على شبكة CDN |
| **Heroku** | منصة سحابية لاستضافة تطبيقات الخوادم الخلفية |
| **الطبقة الكاملة (Full Protection)** | مستوى الحماية الأعلى الذي يجمع بين العلامة المائية + C2PA + التشويش المعاكس للذكاء الاصطناعي |
| **Adversarial Protection** | تقنية حماية تضيف اضطرابات دقيقة غير مرئية تُعيق نماذج الذكاء الاصطناعي من تزييف الصورة |

---

### 1.4 المراجع

- ملف `README.md` في جذر المشروع.
- ملف `DESIGN_SYSTEM.md` – مواصفات التصميم والمكونات البصرية.
- ملف `src/api.ts` – تعريفات واجهة برمجية الخادم الخلفي.
- ملف `.env.example` – متغيرات البيئة المطلوبة.
- معيار C2PA: [https://c2pa.org](https://c2pa.org)

---

## 2. الوصف العام للنظام

### 2.1 منظور النظام

يُشكِّل Iron-ID منظومةً من طرفين رئيسيين:

**1. الواجهة الأمامية (Frontend):**  
تطبيق ويب أحادي الصفحة يُقدِّم واجهة مستخدم تفاعلية وسريعة الاستجابة، مبني بـ React 18 وTypeScript، ومُعبَّأ بـ Vite 5. يعمل على الأجهزة المكتبية والمحمولة، ويدعم التثبيت كتطبيق PWA.

**2. الواجهة الخلفية (Backend API):**  
خدمة REST API مستضافة على Heroku، تُعالج ملفات الوسائط وتُطبِّق خوارزميات الحماية والتحقق. تُوفِّر أيضًا اتصالات WebSocket لتتبع تقدم المعالجة في الوقت الفعلي.

**علاقة النظام مع بيئته:**

```
┌─────────────────────────────────────────────────────┐
│                    المستخدم النهائي                  │
│        (متصفح ويب / تطبيق PWA على الجهاز المحمول)    │
└──────────────────────┬──────────────────────────────┘
                       │ HTTPS
┌──────────────────────▼──────────────────────────────┐
│           الواجهة الأمامية (Vercel CDN)              │
│          React 18 SPA – iron-id frontend             │
│  الصفحات: الرئيسية | الحماية | التحقق | التغذية الراجعة │
└──────────────────────┬──────────────────────────────┘
                       │ REST API + WebSocket
┌──────────────────────▼──────────────────────────────┐
│           الواجهة الخلفية (Heroku)                   │
│  iron-id-ea601dce55ce.herokuapp.com                  │
│  POST /api/protect | POST /api/verify/upload         │
│  GET /api/protect/result/{id} | POST /api/feedback   │
│  WS /api/ws/progress/{jobId}                         │
└─────────────────────────────────────────────────────┘
```

---

### 2.2 وظائف النظام الرئيسية

يُقدِّم النظام أربع وظائف رئيسية:

| الوظيفة | الوصف |
|---------|-------|
| **حماية الوسائط** | رفع ملف وسائط وتطبيق طبقات حماية رقمية متعددة (علامة مائية، C2PA، حماية ضد الذكاء الاصطناعي) |
| **التحقق من الأصالة** | رفع ملف وسائط وفحص مدى أصالته وكشف أي تعديلات طرأت عليه |
| **التغذية الراجعة** | إرسال ملاحظات أو إبلاغ عن أخطاء أو اقتراح ميزات جديدة |
| **عرض المعلومات** | استعراض الميزات التقنية للمنصة وطريقة عملها عبر الصفحة الرئيسية |

---

### 2.3 فئات المستخدمين

يستهدف النظام الفئات التالية:

| الفئة | الوصف | أهمية الاستخدام |
|-------|-------|----------------|
| **الصحفيون والمراسلون** | مهنيو الإعلام الذين يحتاجون إلى إثبات أصالة صورهم ومقاطعهم المصورة قبل النشر | عالية جدًا |
| **المصورون الفوتوغرافيون** | أصحاب حقوق الصور الرقمية الذين يرغبون في حماية أعمالهم ونسبها إليهم | عالية |
| **صُنَّاع الأفلام والمحتوى** | منشئو المحتوى المرئي والمسموع في منصات التواصل والبث | عالية |
| **العلامات التجارية والمؤسسات** | الشركات التي تحتاج إلى ضمان سلامة موادها الإعلانية والدعائية | متوسطة إلى عالية |
| **غرف الأخبار والمؤسسات الإعلامية** | المؤسسات التحريرية التي تتحقق من محتوى يصلها من مصادر خارجية | عالية جدًا |
| **المستخدم العام** | أي شخص يرغب في التحقق من صحة صورة أو مقطع مشكوك فيه | متوسطة |

---

### 2.4 القيود والافتراضات

**القيود:**

| القيد | التوضيح |
|-------|---------|
| **الحد الأقصى لحجم الملف** | 500 ميغابايت لكل عملية رفع |
| **صيغ الملفات للتحقق** | يدعم التحقق صيغتَي JPEG وPNG فقط في الوقت الحالي |
| **الاتصال بالإنترنت** | يتطلب النظام اتصالًا نشطًا بالإنترنت لرفع الملفات ومعالجتها |
| **قدرة الخادم الخلفي** | تعتمد سرعة المعالجة على موارد الخادم المستضاف على Heroku |
| **بيئة المتصفح** | يستلزم استخدامُ جميع الميزات متصفحًا حديثًا يدعم WebSocket وFile API |
| **عدم وجود تسجيل دخول** | لا يوجد حاليًا نظام حسابات أو مصادقة، مما يُقيِّد تخصيص التجربة |

**الافتراضات:**

- يمتلك المستخدم مستعرض ويب حديث يدعم HTML5 وWebSocket وFile API.
- يعمل الخادم الخلفي المستضاف على Heroku ويمكن الوصول إليه عبر الإنترنت.
- يدرك المستخدم أن دقة نتائج التحقق تعتمد على جودة الخوارزميات الخلفية.
- لا تُعدّ نتائج النظام دليلًا قانونيًا رسميًا بذاتها دون الرجوع إلى جهات متخصصة.

---

## 3. المتطلبات الوظيفية

### FR-01: إدارة الصفحة الرئيسية

| المعرف | المتطلب |
|--------|---------|
| FR-01.1 | يجب أن يعرض النظام صفحةً رئيسية تحتوي على: شعار المنصة، قسم "البطل" (Hero) مع العنوان الرئيسي والوصف، وقسم "كيف يعمل" المكوّن من ثلاث خطوات. |
| FR-01.2 | يجب أن يعرض النظام بطاقاتٍ توضيحية لميزاته الرئيسية: أصالة الصورة، كشف التلاعب بالذكاء الاصطناعي، والإثبات التشفيري. |
| FR-01.3 | يجب أن يعرض النظام قسمًا لفئات الجمهور المستهدف (الصحفيون، المصورون، غرف الأخبار، العلامات التجارية، المنشئون). |
| FR-01.4 | يجب أن يتضمن النظام روابط تنقل (CTA) تُوجِّه المستخدم إلى صفحتَي الحماية والتحقق. |
| FR-01.5 | يجب أن يعرض النظام قسم "معرض متعدد الوسائط" يُبرز دعم الأنواع المختلفة (التصوير الفوتوغرافي، السينما، الموسيقى والبودكاست). |

---

### FR-02: حماية ملفات الوسائط

| المعرف | المتطلب |
|--------|---------|
| FR-02.1 | يجب أن يُتيح النظام رفع ملف وسائط عبر السحب والإفلات (Drag & Drop) أو بالنقر لاستعراض الملفات. |
| FR-02.2 | يجب أن يعرض النظام معاينةً للصورة المرفوعة قبل بدء المعالجة. |
| FR-02.3 | يجب أن يدعم النظام الصيغ التالية: صور (JPEG، PNG، GIF، WebP، TIFF)، صوت (MP3، WAV، FLAC، OGG، AAC)، فيديو (MP4، MOV، MKV، WebM، AVI). |
| FR-02.4 | يجب أن يرفض النظام الملفات التي يتجاوز حجمها 500 ميغابايت ويُبلِّغ المستخدم برسالة واضحة. |
| FR-02.5 | يجب أن يُتيح النظام للمستخدم إدخال اسم المؤلف (اختياري) قبل بدء المعالجة. |
| FR-02.6 | يجب أن يُتيح النظام اختيار مستوى الحماية من بين ثلاثة خيارات: علامة مائية فقط، حماية قياسية (علامة مائية + C2PA)، حماية كاملة (حماية معاكسة للذكاء الاصطناعي + علامة مائية + C2PA). |
| FR-02.7 | يجب أن يرسل النظام الملف إلى نقطة نهاية `POST /api/protect` مع بيانات التعريف ومستوى الحماية المختار. |
| FR-02.8 | يجب أن يتتبع النظام تقدم المعالجة في الوقت الفعلي عبر WebSocket (`/api/ws/progress/{jobId}`) ويُعرض للمستخدم كشريط تقدم مع رسالة الحالة. |
| FR-02.9 | يجب أن يعرض النظام بعد اكتمال المعالجة النتائج التقنية التالية: التجزئة الأصلية (SHA-256)، تجزئة الملف المحمي، رمز العلامة المائية، معرف البيان (Manifest ID)، قيمة PSNR، قيمة Delta-E، زمن المعالجة. |
| FR-02.10 | يجب أن يُولِّد النظام رمز QR يحتوي على رابط التحقق من الملف المحمي. |
| FR-02.11 | يجب أن يُتيح النظام تنزيل الملف المحمي مباشرةً. |
| FR-02.12 | يجب أن يُتيح النظام نسخ قيم التجزئة ورمز العلامة المائية إلى الحافظة بنقرة واحدة. |
| FR-02.13 | يجب أن يُتيح النظام بدء عملية حماية ملف جديد بعد إنجاز العملية الحالية. |
| FR-02.14 | يجب أن يُعرض النظام رسالة خطأ واضحة وخيار إعادة المحاولة في حالة فشل المعالجة. |

---

### FR-03: التحقق من أصالة ملفات الوسائط

| المعرف | المتطلب |
|--------|---------|
| FR-03.1 | يجب أن يُتيح النظام رفع ملف للتحقق عبر السحب والإفلات أو النقر لاستعراض الملفات. |
| FR-03.2 | يجب أن يقتصر التحقق على ملفات الصور بصيغتَي JPEG وPNG فقط. |
| FR-03.3 | يجب أن يرسل النظام الملف إلى نقطة نهاية `POST /api/verify/upload` ويُعالج الاستجابة. |
| FR-03.4 | يجب أن يعرض النظام حالةً واضحة للنتيجة ضمن أربع حالات: **أصيل** (Authentic)، **مُعدَّل** (Modified)، **مشبوه** (Suspect)، **غير مُتحقَّق** (Unverified)، مع ترميز لوني مميز لكل حالة. |
| FR-03.5 | يجب أن يُبيِّن النظام ما إذا اكتُشفت علامة مائية ونسبة الثقة في الاكتشاف (0–100%). |
| FR-03.6 | يجب أن يُبيِّن النظام ما إذا كان التوقيع C2PA صحيحًا. |
| FR-03.7 | يجب أن يعرض النظام معلومات المؤلف وتاريخ الإنشاء الأصلي إن توافرا في البيانات الوصفية. |
| FR-03.8 | يجب أن يُشير النظام بوضوح إلى ما إذا اكتُشفت تعديلات على الملف. |
| FR-03.9 | يجب أن يُتيح النظام عرض التفاصيل التقنية الكاملة للنتيجة في قسم قابل للطي. |
| FR-03.10 | يجب أن يُتيح النظام التحقق من ملف آخر بعد إنجاز العملية الحالية. |
| FR-03.11 | يجب أن يُعرض النظام رسالة خطأ واضحة وخيار إعادة المحاولة في حالة فشل التحقق. |

---

### FR-04: إرسال التغذية الراجعة

| المعرف | المتطلب |
|--------|---------|
| FR-04.1 | يجب أن يُتيح النظام للمستخدم تعبئة نموذج تغذية راجعة يحتوي على: الاسم (نص، بحد أقصى 200 حرف، مطلوب)، البريد الإلكتروني (مطلوب)، الرسالة (نص، بحد أقصى 2000 حرف، مطلوب). |
| FR-04.2 | يجب أن يعرض النظام عدادًا حيًا لعدد الحروف المتبقية لحقل الرسالة. |
| FR-04.3 | يجب أن يرسل النظام البيانات إلى `POST /api/feedback` عند نقر المستخدم على زر الإرسال. |
| FR-04.4 | يجب أن يُعطِّل النظام زر الإرسال أثناء الإرسال ويُعرض مؤشر تحميل. |
| FR-04.5 | يجب أن يعرض النظام رسالة نجاح خضراء ويُعيد ضبط النموذج عند الإرسال الناجح. |
| FR-04.6 | يجب أن يعرض النظام رسالة خطأ حمراء مع إمكانية إعادة المحاولة في حالة الفشل. |

---

### FR-05: التنقل والتخطيط

| المعرف | المتطلب |
|--------|---------|
| FR-05.1 | يجب أن يُوفِّر النظام شريط تنقل علويًا ثابتًا يظهر على جميع الصفحات. |
| FR-05.2 | يجب أن يتضمن شريط التنقل الروابط التالية: الرئيسية، الحماية، التحقق، التغذية الراجعة. |
| FR-05.3 | يجب أن يُوفِّر النظام شريط تنقل سفليًا للأجهزة المحمولة يحتوي على الروابط الرئيسية الثلاثة: الرئيسية، الحماية، التحقق. |
| FR-05.4 | يجب أن يُبرِّز النظام الرابط النشط في شريط التنقل بلون مميز. |
| FR-05.5 | يجب أن يُوفِّر النظام تذييلًا (Footer) على أجهزة الكمبيوتر يتضمن حق النشر ورابطًا للتغذية الراجعة. |

---

### FR-06: التدويل ودعم اللغات

| المعرف | المتطلب |
|--------|---------|
| FR-06.1 | يجب أن يدعم النظام ثلاث لغات: الفرنسية، الإنجليزية، العربية. |
| FR-06.2 | يجب أن يكتشف النظام لغة المتصفح تلقائيًا عند أول زيارة ويضبطها كلغة افتراضية. |
| FR-06.3 | يجب أن يُتيح النظام تبديل اللغة في أي وقت عبر أزرار اختيار اللغة في شريط التنقل. |
| FR-06.4 | يجب أن يطبق النظام تنسيق الكتابة من اليمين إلى اليسار (RTL) تلقائيًا عند اختيار اللغة العربية. |
| FR-06.5 | يجب أن يستخدم النظام اللغة الإنجليزية كلغة احتياطية (fallback) عند عدم وجود ترجمة لمفتاح معين. |

---

### FR-07: التطبيق التدريجي للويب (PWA)

| المعرف | المتطلب |
|--------|---------|
| FR-07.1 | يجب أن يكون النظام قابلًا للتثبيت على الأجهزة المحمولة وأجهزة الكمبيوتر كتطبيق PWA. |
| FR-07.2 | يجب أن يُوفِّر النظام عامل خدمة (Service Worker) يُمكِّن التخزين المؤقت للأصول الثابتة. |
| FR-07.3 | يجب أن يُوفِّر النظام اختصارات تطبيق مباشرة لصفحتَي الحماية والتحقق. |

---

## 4. المتطلبات غير الوظيفية

### 4.1 الأداء

| المعرف | المتطلب |
|--------|---------|
| NFR-PERF-01 | يجب أن يُكمِل النظام تحميلَ الصفحة الأولية في أقل من 3 ثوانٍ على شبكة الجيل الرابع (4G). |
| NFR-PERF-02 | يجب أن تستجيب واجهة المستخدم للتفاعلات (نقر، تمرير) في أقل من 100 ميلي ثانية. |
| NFR-PERF-03 | يجب أن تعمل الحركات البصرية والرسوم المتحركة بمعدل 60 إطارًا في الثانية على الأجهزة المتوسطة الأداء. |
| NFR-PERF-04 | يجب أن يُقسِّم النظام حِزمَه (Code Splitting) لتجنب تحميل الكود غير المستخدم: حزمة Framer Motion منفصلة، وحزمة المكتبات الخارجية (vendor) منفصلة. |
| NFR-PERF-05 | يجب ألا يتجاوز حجم الحزمة الرئيسية تحذير 600 كيلوبايت المُحدَّد في إعدادات Vite. |
| NFR-PERF-06 | يجب أن يُوفِّر النظام تتبعًا حيًا لتقدم رفع الملفات ومعالجتها عبر WebSocket بتحديثات لا تتجاوز 500 ميلي ثانية. |

---

### 4.2 الأمان

| المعرف | المتطلب |
|--------|---------|
| NFR-SEC-01 | يجب أن تتم جميع الاتصالات مع الخادم الخلفي عبر HTTPS في بيئة الإنتاج. |
| NFR-SEC-02 | يجب أن يتحقق النظام من نوع MIME وحجم الملف قبل الرفع لمنع رفع ملفات ضارة. |
| NFR-SEC-03 | يجب ألا يخزن النظام أي بيانات حساسة (هاشات، رموز) في التخزين المحلي (localStorage) بشكل دائم. |
| NFR-SEC-04 | يجب التحقق من صحة بيانات نموذج التغذية الراجعة (البريد الإلكتروني، الحقول المطلوبة) قبل الإرسال. |
| NFR-SEC-05 | يجب أن تُقرأ عنوان URL الخاص بالخادم الخلفي من متغيرات البيئة (`VITE_API_URL`) ولا تُضمَّن بشكل ثابت في الكود. |
| NFR-SEC-06 | يجب أن يُعالج النظام أخطاء الشبكة والاستجابات الفاشلة بشكل آمن دون الكشف عن معلومات داخلية للمستخدم. |

---

### 4.3 القابلية للاستخدام

| المعرف | المتطلب |
|--------|---------|
| NFR-USE-01 | يجب أن تتوافق الواجهة مع معايير الوصول WCAG 2.1 المستوى AA. |
| NFR-USE-02 | يجب أن يكون الحد الأدنى لحجم منطقة النقر (Touch Target) 48×48 بكسل على الأجهزة المحمولة. |
| NFR-USE-03 | يجب أن يُوفِّر النظام سمات ARIA المناسبة على جميع العناصر التفاعلية. |
| NFR-USE-04 | يجب أن يدعم النظام التنقل بلوحة المفاتيح في جميع الوظائف. |
| NFR-USE-05 | يجب أن تكون نسب تباين الألوان مطابقةً لمعايير WCAG AA على الأقل. |
| NFR-USE-06 | يجب أن تُبيِّن رسائل الخطأ ما الذي حدث وكيفية تصحيحه بلغة واضحة ومفهومة. |
| NFR-USE-07 | يجب أن تكون واجهة المستخدم مستجيبةً تمامًا (Fully Responsive) لجميع أحجام الشاشات من 320 بكسل وأكثر. |
| NFR-USE-08 | يجب أن يعكس النظام تغيير اللغة فوريًا دون إعادة تحميل الصفحة. |
| NFR-USE-09 | يجب أن يُنظِّم النظام التخطيط بشكل صحيح في اتجاه RTL عند تفعيل اللغة العربية (محاذاة النصوص والعناصر من اليمين). |

---

### 4.4 القابلية للتوسع

| المعرف | المتطلب |
|--------|---------|
| NFR-SCAL-01 | يجب أن تكون بنية الكود معيارية بحيث يمكن إضافة صفحات ووظائف جديدة دون التأثير على الوظائف القائمة. |
| NFR-SCAL-02 | يجب أن يكون نظام الترجمة (i18n) قابلًا لإضافة لغات جديدة بإضافة قاموس ترجمة واحد. |
| NFR-SCAL-03 | يجب أن يكون نظام API قابلًا لإضافة نقاط نهاية جديدة عبر الملف `src/api.ts` دون إعادة بناء المكتبات الأساسية. |
| NFR-SCAL-04 | يجب أن تعتمد مكتبة المكونات على مبدأ الاستقلالية لتيسير إعادة الاستخدام في صفحات مستقبلية. |
| NFR-SCAL-05 | يجب أن يدعم نظام الاستضافة (Vercel) التوسعَ التلقائي لاستيعاب الزيادة في عدد المستخدمين. |

---

## 5. مخططات حالات الاستخدام

### 5.1 نظرة عامة على حالات الاستخدام

```
┌─────────────────────────────────────────────────────────────────┐
│                         نظام Iron-ID                            │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   حالات الاستخدام الرئيسية               │   │
│  │                                                          │   │
│  │   ┌────────────────┐    ┌─────────────────────────────┐  │   │
│  │   │ UC-01          │    │ UC-02                       │  │   │
│  │   │ استعراض الصفحة │    │ حماية ملف وسائط             │  │   │
│  │   │ الرئيسية       │    │                             │  │   │
│  │   └────────────────┘    └─────────────────────────────┘  │   │
│  │                                                          │   │
│  │   ┌────────────────┐    ┌─────────────────────────────┐  │   │
│  │   │ UC-03          │    │ UC-04                       │  │   │
│  │   │ التحقق من      │    │ إرسال تغذية راجعة           │  │   │
│  │   │ أصالة ملف     │    │                             │  │   │
│  │   └────────────────┘    └─────────────────────────────┘  │   │
│  │                                                          │   │
│  │   ┌────────────────┐    ┌─────────────────────────────┐  │   │
│  │   │ UC-05          │    │ UC-06                       │  │   │
│  │   │ تبديل اللغة   │    │ تنزيل الملف المحمي          │  │   │
│  │   └────────────────┘    └─────────────────────────────┘  │   │
│  │                                                          │   │
│  └──────────────────────────────────────────────────────────┘   │
│                             ▲                                   │
│                             │                                   │
│              ┌──────────────┴──────────────┐                   │
│              │         المستخدم            │                   │
│              └─────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────────┘
```

---

### 5.2 توصيف حالات الاستخدام

#### UC-01: استعراض الصفحة الرئيسية

| الحقل | القيمة |
|-------|--------|
| **المعرف** | UC-01 |
| **الاسم** | استعراض الصفحة الرئيسية |
| **الممثلون** | المستخدم |
| **الوصف** | يُطَّلع المستخدم على ميزات المنصة وكيفية عملها |
| **التدفق الرئيسي** | 1. يفتح المستخدم التطبيق. 2. يُعرض قسم البطل مع العنوان والوصف. 3. يتصفح المستخدم بطاقات الميزات وقسم الخطوات الثلاث. 4. يضغط على زر CTA للانتقال إلى الحماية أو التحقق. |
| **الشرط المسبق** | لا يوجد |
| **نتيجة النجاح** | يصل المستخدم إلى الصفحة المطلوبة |

---

#### UC-02: حماية ملف وسائط

| الحقل | القيمة |
|-------|--------|
| **المعرف** | UC-02 |
| **الاسم** | حماية ملف وسائط |
| **الممثلون** | المستخدم، الخادم الخلفي |
| **الوصف** | يرفع المستخدم ملف وسائط لإضافة طبقات حماية رقمية عليه |
| **التدفق الرئيسي** | 1. ينتقل المستخدم إلى صفحة الحماية. 2. يسحب ملفًا أو ينقر للاختيار. 3. يُعاين الملف المرفوع. 4. يختار مستوى الحماية وإدخال اسم المؤلف اختياريًا. 5. يضغط على زر الحماية. 6. يتابع شريط التقدم والرسائل الحية. 7. يستعرض النتائج التقنية. 8. يُنزِّل الملف المحمي أو ينسخ القيم. |
| **الشرط المسبق** | ملف بصيغة مدعومة وحجم ≤ 500 ميغابايت |
| **نتيجة النجاح** | ملف محمي جاهز للتنزيل مع بيانات التحقق |
| **التدفق البديل** | فشل المعالجة: عرض رسالة الخطأ وزر إعادة المحاولة |

---

#### UC-03: التحقق من أصالة ملف

| الحقل | القيمة |
|-------|--------|
| **المعرف** | UC-03 |
| **الاسم** | التحقق من أصالة ملف |
| **الممثلون** | المستخدم، الخادم الخلفي |
| **الوصف** | يرفع المستخدم ملفًا للتحقق من أصالته وكشف أي تعديلات |
| **التدفق الرئيسي** | 1. ينتقل المستخدم إلى صفحة التحقق. 2. يسحب صورة أو ينقر للاختيار (JPEG/PNG فقط). 3. يضغط على زر التحقق. 4. ينتظر انتهاء التحليل. 5. يستعرض النتيجة (أصيل / مُعدَّل / مشبوه / غير مُتحقَّق) مع التفاصيل. |
| **الشرط المسبق** | ملف صورة بصيغة JPEG أو PNG |
| **نتيجة النجاح** | نتيجة تحقق واضحة مع تفاصيل تقنية |
| **التدفق البديل** | فشل التحقق: عرض رسالة الخطأ وزر إعادة المحاولة |

---

#### UC-04: إرسال تغذية راجعة

| الحقل | القيمة |
|-------|--------|
| **المعرف** | UC-04 |
| **الاسم** | إرسال تغذية راجعة |
| **الممثلون** | المستخدم، الخادم الخلفي |
| **الوصف** | يُرسل المستخدم ملاحظاته أو بلاغًا عن خطأ أو اقتراحًا |
| **التدفق الرئيسي** | 1. ينتقل المستخدم إلى صفحة التغذية الراجعة. 2. يُعبئ الاسم والبريد الإلكتروني والرسالة. 3. يضغط على زر الإرسال. 4. يُعرض مؤشر التحميل. 5. يُعرض رسالة النجاح ويُعاد ضبط النموذج. |
| **الشرط المسبق** | بريد إلكتروني صالح، جميع الحقول المطلوبة مُعبَّأة |
| **نتيجة النجاح** | تأكيد إرسال الرسالة |
| **التدفق البديل** | خطأ إرسال: عرض رسالة خطأ مع إمكانية المحاولة مرة أخرى |

---

#### UC-05: تبديل اللغة

| الحقل | القيمة |
|-------|--------|
| **المعرف** | UC-05 |
| **الاسم** | تبديل لغة الواجهة |
| **الممثلون** | المستخدم |
| **الوصف** | يُغيِّر المستخدم لغة الواجهة من شريط التنقل |
| **التدفق الرئيسي** | 1. يضغط المستخدم على زر اللغة (FR / EN / AR). 2. تتحول الواجهة فورًا إلى اللغة المختارة. 3. عند اختيار العربية، يُطبَّق اتجاه RTL. |
| **الشرط المسبق** | لا يوجد |
| **نتيجة النجاح** | الواجهة بالكامل باللغة المختارة مع اتجاه الكتابة الصحيح |

---

#### UC-06: تنزيل الملف المحمي

| الحقل | القيمة |
|-------|--------|
| **المعرف** | UC-06 |
| **الاسم** | تنزيل الملف المحمي |
| **الممثلون** | المستخدم، الخادم الخلفي |
| **الوصف** | يُنزِّل المستخدم الملف المحمي بعد إنجاز عملية الحماية |
| **التدفق الرئيسي** | 1. تكتمل عملية الحماية. 2. يضغط المستخدم على زر التنزيل. 3. يُبنى مسار التنزيل تلقائيًا من `output_path`. 4. يبدأ تنزيل الملف من الخادم (`GET /uploads/{filename}`). |
| **الشرط المسبق** | إنجاز عملية الحماية بنجاح |
| **نتيجة النجاح** | تنزيل الملف المحمي على جهاز المستخدم |

---

## 6. هيكلية النظام المقترحة

### 6.1 الهيكل العام

```
                     ┌──────────────────────────────────┐
                     │        المستخدم النهائي           │
                     │   (متصفح + PWA)                  │
                     └────────────┬─────────────────────┘
                                  │ HTTPS / WSS
                     ┌────────────▼─────────────────────┐
                     │      Vercel CDN (الحافة)          │
                     │  التوجيه + الضغط + HTTPS          │
                     └────────────┬─────────────────────┘
                                  │
        ┌─────────────────────────▼──────────────────────────────┐
        │                الواجهة الأمامية (Frontend)              │
        │                     React 18 SPA                        │
        │                                                         │
        │  ┌──────────────────────────────────────────────────┐  │
        │  │                 طبقة التوجيه (React Router)       │  │
        │  │  /  →  HomePage   /protect  →  ProtectPage       │  │
        │  │  /verify  →  VerifyPage   /feedback  →  Feedback │  │
        │  └──────────────────────────────────────────────────┘  │
        │                                                         │
        │  ┌─────────────────┐  ┌────────────────────────────┐  │
        │  │  طبقة الحالة    │  │  طبقة التدويل (i18n)       │  │
        │  │  (React State)  │  │  FR / EN / AR + RTL        │  │
        │  └─────────────────┘  └────────────────────────────┘  │
        │                                                         │
        │  ┌─────────────────────────────────────────────────┐  │
        │  │             طبقة API Client (api.ts)            │  │
        │  │  protectFile() | verifyFile() | sendFeedback()  │  │
        │  │  WebSocket Manager (progress tracking)          │  │
        │  └─────────────────────────────────────────────────┘  │
        └────────────────────────────────────────────────────────┘
                                  │ REST API + WebSocket
        ┌─────────────────────────▼──────────────────────────────┐
        │                الواجهة الخلفية (Backend)                │
        │         Heroku – iron-id-ea601dce55ce.herokuapp.com     │
        │                                                         │
        │  ┌──────────────────────────────────────────────────┐  │
        │  │                نقاط النهاية (Endpoints)           │  │
        │  │  POST /api/protect           (رفع للحماية)        │  │
        │  │  GET  /api/protect/result/{id} (نتيجة الحماية)    │  │
        │  │  POST /api/verify/upload     (التحقق)             │  │
        │  │  POST /api/feedback          (التغذية الراجعة)    │  │
        │  │  WS   /api/ws/progress/{id}  (التقدم الحي)        │  │
        │  │  GET  /uploads/{filename}    (تنزيل الملف)        │  │
        │  └──────────────────────────────────────────────────┘  │
        │                                                         │
        │  ┌──────────────────────────────────────────────────┐  │
        │  │            محركات المعالجة                       │  │
        │  │  🔵 محرك العلامة المائية  (Watermark Engine)     │  │
        │  │  🟣 محرك C2PA             (Manifest Signing)     │  │
        │  │  🔴 محرك الحماية          (Adversarial Engine)   │  │
        │  │  🟢 محرك التحقق           (Verification Engine) │  │
        │  └──────────────────────────────────────────────────┘  │
        └────────────────────────────────────────────────────────┘
```

---

### 6.2 هيكل ملفات الواجهة الأمامية

```
src/
├── main.tsx              # نقطة دخول – تهيئة React Router وI18n
├── App.tsx               # التخطيط العام – شريط التنقل والتذييل
├── i18n.tsx              # نظام الترجمة ومزود السياق
├── api.ts                # عميل API + مدير WebSocket
├── index.css             # الأنماط العالمية ومكتبة المكونات
└── pages/
    ├── HomePage.tsx      # الصفحة الرئيسية (1580+ سطر)
    ├── ProtectPage.tsx   # صفحة الحماية
    ├── VerifyPage.tsx    # صفحة التحقق
    └── FeedbackPage.tsx  # صفحة التغذية الراجعة
```

---

### 6.3 نماذج البيانات

#### نموذج طلب الحماية

```typescript
// طلب الحماية (ProtectRequest)
FormData {
  file: File,                           // الملف المراد حمايته
  metadata: JSON.stringify({
    author?: string                     // اسم المؤلف (اختياري)
  }),
  protection_level:                     // مستوى الحماية
    "standard" | "full" | "watermark_only"
}
```

#### نموذج نتيجة الحماية

```typescript
// نتيجة الحماية (JobResult)
{
  output_path: string,       // مسار الملف المحمي على الخادم
  original_hash: string,     // تجزئة SHA-256 للملف الأصلي
  protected_hash: string,    // تجزئة SHA-256 للملف المحمي
  watermark_token: string,   // رمز العلامة المائية
  manifest_id: string,       // معرف بيان C2PA
  psnr: number | null,       // نسبة الإشارة إلى الضوضاء (ديسيبل)
  delta_e: number | null,    // مقياس الفارق اللوني
  processing_time: number | null, // زمن المعالجة (ثواني)
  layers_applied: string[],  // طبقات الحماية المطبقة
  file_type: string,         // نوع الملف
  error: string | null,      // رسالة الخطأ إن وُجدت
  qr_code_base64?: string    // رمز QR مُشفَّر بـ base64
}
```

#### نموذج نتيجة التحقق

```typescript
// نتيجة التحقق (VerifyResponse)
{
  status: "authentic" | "modified" | "unverified" | "suspect",
  watermark_found: boolean,          // هل اكتُشفت علامة مائية؟
  watermark_confidence: number,      // نسبة الثقة (0 إلى 1)
  c2pa_valid: boolean,               // هل التوقيع C2PA صحيح؟
  author: string | null,             // اسم المؤلف
  original_date: string | null,      // تاريخ الإنشاء الأصلي
  modifications_detected: boolean,   // هل اكتُشفت تعديلات؟
  details: Record<string, unknown>   // تفاصيل تقنية إضافية
}
```

#### نموذج تحديث تقدم WebSocket

```typescript
// رسالة WebSocket (ProgressMessage)
{
  step: string,        // اسم خطوة المعالجة الحالية
  progress: number,    // نسبة التقدم (0–100)
  message: string,     // رسالة الحالة
  result?: JobResult   // النتيجة النهائية (عند step === "done")
}
```

---

### 6.4 نظام التصميم البصري

| العنصر | القيمة |
|--------|--------|
| **نمط التصميم** | داكن حصرًا (Dark Mode Only) |
| **لون الخلفية الأساسية** | `#050505` (أسود فحمي) |
| **اللون الأزرق النيوني** | `#3B82F6` (الأزرار، التركيز) |
| **اللون البنفسجي الكهربائي** | `#8B5CF6` (التمييز الثانوي) |
| **نص رئيسي** | `#F8FAFC` |
| **نص ثانوي** | `#94A3B8` |
| **خط العنوان** | Space Grotesk |
| **خط المحتوى** | Inter |
| **خط الكود** | JetBrains Mono |
| **محرك الرسوم المتحركة** | Framer Motion 12 |
| **إطار CSS** | Tailwind CSS 3 |

---

## 7. السيناريوهات الرئيسية لاستخدام النظام

### السيناريو 1: صحفي يُؤمِّن صوره قبل النشر

**الوصف:**  
صحفيٌّ ميداني التقط صورًا حصرية لحدث مهم ويريد إثبات أصالتها قبل نشرها في وسيلة إعلامية.

**الخطوات:**
1. يفتح الصحفي تطبيق Iron-ID على هاتفه المحمول.
2. ينتقل إلى صفحة **الحماية**.
3. يسحب صورته إلى منطقة الرفع أو ينقر لاستعراضها.
4. تظهر معاينة الصورة ومعلوماتها (الاسم، الحجم، الصيغة).
5. يُكشِف عن خيارات الحماية ويُدخل اسمه كمؤلف.
6. يختار مستوى الحماية **الكامل** (Full Protection).
7. يضغط على زر **"حماية الملف"**.
8. يتابع شريط التقدم: "تحليل الملف... 25%" ← "تطبيق العلامة المائية... 50%" ← "توليد بيان C2PA... 75%" ← "اكتملت المعالجة... 100%".
9. تظهر نتيجة الحماية: طبقات مطبقة، تجزئة SHA-256، PSNR = 42 ديسيبل، وقت المعالجة = 3.2 ثانية.
10. يُنزِّل الصورة المحمية ويُرسلها إلى محرر الجريدة.
11. يُرسل رمز QR إلى محرره ليتحقق من أصالتها.

**النتيجة:** الصورة محمية بثلاث طبقات رقمية مع بصمة تشفيرية تثبت هوية الصحفي ووقت الالتقاط.

---

### السيناريو 2: محرر يتحقق من صورة مشبوهة

**الوصف:**  
محررٌ في غرفة أخبار تصله صورة من مصدر مجهول وهو متشكك في أصالتها قبل نشرها.

**الخطوات:**
1. يفتح المحرر تطبيق Iron-ID على متصفح الكمبيوتر.
2. ينتقل إلى صفحة **التحقق**.
3. يسحب الصورة إلى منطقة الرفع (JPEG بحجم 3 ميغابايت).
4. تظهر معاينة الصورة.
5. يضغط على زر **"التحقق من الأصالة"**.
6. تظهر نتيجة التحليل بعد 2 ثانية:
   - الحالة: 🟡 **مُعدَّلة (Modified)** (خلفية عنبرية)
   - العلامة المائية: **موجودة** بثقة 94%
   - توقيع C2PA: **غير صحيح** (تعارض بين الملف والبيان)
   - التعديلات المكتشفة: **نعم**
   - المؤلف: Ahmad Benali
   - التاريخ الأصلي: 2025-11-14
7. يُكشِف عن التفاصيل التقنية لمراجعة البيانات الكاملة.
8. يقرر المحرر عدم نشر الصورة ويتواصل مع المصدر للتوضيح.

**النتيجة:** كشف النظام عن تعديلات طرأت على الصورة مما أنقذ المؤسسة الإعلامية من نشر محتوى مزيف.

---

### السيناريو 3: مصورٌ فوتوغرافي يحمي معرضه الفني

**الوصف:**  
مصورٌ فوتوغرافي يريد حماية مجموعة لقطاته الفنية من الاستخدام غير المشروع والادعاء بملكيتها.

**الخطوات:**
1. يفتح المصور التطبيق ويختار اللغة الإنجليزية من زر **EN** في شريط التنقل.
2. ينتقل إلى صفحة **Protect**.
3. يرفع صورته (PNG بحجم 25 ميغابايت).
4. يُدخل اسمه كمؤلف: "Youssef Khadraoui Photography".
5. يختار مستوى الحماية **القياسي** (Standard: Watermark + C2PA).
6. يضغط زر الحماية ويتابع التقدم.
7. تكتمل المعالجة:
   - رمز العلامة المائية: `wm_a7f3d2...`
   - معرف البيان: `urn:c2pa:ab12cd34...`
   - Delta-E: 0.3 (فارق لوني لا يُرى بالعين)
8. ينسخ معرف البيان ويُضيفه إلى بيانات معرضه.
9. يُنزِّل الصورة المحمية.
10. يضم رمز QR في الطباعة المادية للصورة.

**النتيجة:** الصورة تحمل توقيعًا رقميًا دائمًا يربطها بالمصور، وأي محكمة أو منصة تحقق يمكنها التثبت من ذلك.

---

### السيناريو 4: مستخدم عربي يتحقق من محتوى مشبوه

**الوصف:**  
مستخدم عربي يتلقى مقطع فيديو مُزعَم أنه أصيل ويريد التحقق منه.

**الخطوات:**
1. يفتح المستخدم التطبيق. يُكتشف تلقائيًا أن لغة المتصفح عربية.
2. تظهر الواجهة بالعربية مع تخطيط من اليمين إلى اليسار (RTL).
3. يلاحظ المستخدم أن التحقق يدعم صيغتَي JPEG وPNG فقط، فيُحوِّل إطار الفيديو إلى صورة PNG.
4. ينتقل إلى صفحة **تحقق** من شريط التنقل السفلي.
5. يسحب الصورة إلى منطقة الرفع.
6. يضغط على زر **"التحقق من الأصالة"**.
7. تظهر النتيجة:
   - الحالة: 🔴 **مشبوه (Suspect)**
   - العلامة المائية: **غير موجودة**
   - توقيع C2PA: **غير موجود**
   - التعديلات: **غير محددة** (لا بيانات مرجعية)
8. يقرر المستخدم عدم مشاركة المقطع وإبلاغ المنصة.

**النتيجة:** حماية المستخدم من نشر معلومات مضللة.

---

### السيناريو 5: مستخدم يُبلِّغ عن خطأ في النظام

**الوصف:**  
مستخدم واجه مشكلة تقنية أثناء استخدام التطبيق ويريد الإبلاغ عنها للفريق التقني.

**الخطوات:**
1. يضغط المستخدم على رابط **تعليقات** في شريط التنقل.
2. ينتقل إلى صفحة التغذية الراجعة.
3. يُعبئ النموذج:
   - الاسم: "Meriem Djamel"
   - البريد الإلكتروني: "meriem@example.com"
   - الرسالة: "عند رفع ملف PNG بحجم 12 ميغابايت، تظهر رسالة خطأ: 'Internal Server Error' بعد انتظار 30 ثانية. أرجو المعالجة."
4. يلاحظ العداد: 145/2000 حرف.
5. يضغط على زر **"إرسال"**.
6. يظهر مؤشر التحميل ثم رسالة النجاح الخضراء.
7. يُعاد ضبط النموذج تلقائيًا.

**النتيجة:** وصول البلاغ إلى الفريق التقني لمعالجة المشكلة وتحسين المنصة.

---

## ملحق أ: مصفوفة تتبع المتطلبات

| رقم المتطلب | الوصف المختصر | الصفحة المنفِّذة | حالة التنفيذ |
|------------|--------------|-----------------|--------------|
| FR-01.1–5 | الصفحة الرئيسية | `HomePage.tsx` | ✅ منجز |
| FR-02.1–14 | حماية الملفات | `ProtectPage.tsx` | ✅ منجز |
| FR-03.1–11 | التحقق من الأصالة | `VerifyPage.tsx` | ✅ منجز |
| FR-04.1–6 | التغذية الراجعة | `FeedbackPage.tsx` | ✅ منجز |
| FR-05.1–5 | التنقل والتخطيط | `App.tsx` | ✅ منجز |
| FR-06.1–5 | التدويل ودعم اللغات | `i18n.tsx` | ✅ منجز |
| FR-07.1–3 | PWA | `vite.config.ts` | ✅ منجز |
| NFR-PERF-01–06 | متطلبات الأداء | البنية التحتية | ✅ منجز |
| NFR-SEC-01–06 | متطلبات الأمان | `api.ts` | ✅ منجز |
| NFR-USE-01–09 | قابلية الاستخدام | جميع المكونات | ✅ منجز |
| NFR-SCAL-01–05 | قابلية التوسع | البنية العامة | ✅ منجز |

---

## ملحق ب: مواصفات بيئة التشغيل

| البيئة | التفاصيل |
|--------|----------|
| **بيئة التطوير** | Node.js 18+، `npm run dev` → خادم Vite على المنفذ 5173 |
| **بيئة البناء** | `npm run build` → TypeScript تجميع + Vite حزم |
| **معاينة الإنتاج** | `npm run preview` أو `node_modules/.bin/vite preview` |
| **بيئة الإنتاج** | Vercel CDN (واجهة أمامية) + Heroku (خلفية) |
| **متغيرات البيئة** | `VITE_API_URL` – عنوان URL للخادم الخلفي |
| **الحاوية Docker** | `Dockerfile` متاح لنشر الواجهة الأمامية في حاوية |

---

*انتهت وثيقة SRS – الإصدار 1.0*  
*جميع المتطلبات مستخرجة من كود المصدر الفعلي للمشروع.*
